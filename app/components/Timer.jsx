var React = require('react');

var Timer = React.createClass({
    render: function() {
        return (
            <div>
                <p>Timer page</p>
            </div>
        );
    }
});

module.exports = Timer;